# v1.0.0 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publicar a versão 1.0.0 do PDF Tools com bump de versão, renome do EXE, atualização da aba Sobre e publicação da release no GitHub.

**Architecture:** Todas as mudanças são em `pdf_ocr.py` e `pdf_ocr.spec`. Sem novos arquivos. Após o commit, a tag `v1.0.0` dispara o GitHub Actions que compila e publica automaticamente.

**Tech Stack:** Python, Tkinter, PyInstaller, GitHub Actions

---

## Arquivos Modificados

- Modify: `pdf_ocr.py` — version bump, aba Sobre (descrições e card changelog)
- Modify: `pdf_ocr.spec` — renomear EXE de `PDF_OCR` para `PDF_Tools`

---

## Task 1: Version bump e renome do EXE

**Files:**
- Modify: `pdf_ocr.py` (linha ~35)
- Modify: `pdf_ocr.spec` (linha ~79)

- [ ] **Step 1: Atualizar `APP_VERSION` em `pdf_ocr.py`**

Localizar a linha:
```python
APP_VERSION = "0.9.7"
```
Substituir por:
```python
APP_VERSION = "1.0.0"
```

- [ ] **Step 2: Renomear EXE em `pdf_ocr.spec`**

Localizar a linha:
```python
    name='PDF_OCR',
```
Substituir por:
```python
    name='PDF_Tools',
```

- [ ] **Step 3: Verificar sintaxe do Python**

Executar a partir de `c:\Users\nicol\Downloads\pdf_ocr`:
```bash
python -c "import ast; ast.parse(open('pdf_ocr.py', encoding='utf-8').read()); print('OK')"
```
Esperado: `OK`

- [ ] **Step 4: Commit**

```bash
git add pdf_ocr.py pdf_ocr.spec
git commit -m "chore: bump version to 1.0.0, rename EXE to PDF_Tools"
```

---

## Task 2: Atualizar descrições na aba Sobre

**Files:**
- Modify: `pdf_ocr.py` (método `_build_about_page`, grade de funcionalidades ~linha 1841)

A grade atual tem "Em breve." nas descrições de Dividir e Juntar. Substituir pelo bloco completo atualizado.

- [ ] **Step 1: Localizar e substituir o bloco `_features` em `_build_about_page`**

Localizar:
```python
        _features = [
            ("⬡", "OCR",          "Converte PDFs escaneados em pesquisáveis,\n"
                                   "com destaque automático de nomes de pessoas."),
            ("⊜", "Comprimir",    "Reduz o tamanho de PDFs com opções de\n"
                                   "qualidade (100–250 DPI) e formato (JPEG/PNG)."),
            ("⊟", "Dividir PDF",  "Separa um PDF em partes individuais.\n"
                                   "Em breve."),
            ("⊞", "Juntar PDF",   "Une múltiplos PDFs em um único arquivo.\n"
                                   "Em breve."),
        ]
```

Substituir por:
```python
        _features = [
            ("⬡", "OCR",          "Converte PDFs escaneados em pesquisáveis,\n"
                                   "com destaque automático de nomes de pessoas."),
            ("⊜", "Comprimir",    "Reduz o tamanho de PDFs com opções de\n"
                                   "qualidade (100–250 DPI) e formato (JPEG/PNG)."),
            ("⊟", "Dividir PDF",  "Separa um PDF por intervalo único, múltiplos\n"
                                   "intervalos ou todas as páginas individualmente."),
            ("⊞", "Juntar PDF",   "Une múltiplos PDFs em ordem personalizável,\n"
                                   "com drag & drop e reordenação por ↑↓."),
        ]
```

- [ ] **Step 2: Verificar sintaxe**

```bash
python -c "import ast; ast.parse(open('pdf_ocr.py', encoding='utf-8').read()); print('OK')"
```
Esperado: `OK`

- [ ] **Step 3: Commit**

```bash
git add pdf_ocr.py
git commit -m "feat: update About page feature descriptions for v1.0.0"
```

---

## Task 3: Adicionar card "O que há de novo" na aba Sobre

**Files:**
- Modify: `pdf_ocr.py` (método `_build_about_page`, após o card de atualização)

- [ ] **Step 1: Localizar o fim de `_build_about_page`**

O método termina com `self.after(1500, self._auto_update_check)`. Inserir o card de changelog **antes** dessa linha.

- [ ] **Step 2: Inserir o card de changelog**

Localizar:
```python
        self.after(1500, self._auto_update_check)
```

Substituir por:
```python
        # card changelog
        cl_card = tk.Frame(page, bg=C["panel"],
                           highlightthickness=1,
                           highlightbackground=C["border"])
        cl_card.pack(fill="x", padx=24, pady=(12, 0))

        cl_inner = tk.Frame(cl_card, bg=C["panel"], padx=20, pady=16)
        cl_inner.pack(fill="x")

        tk.Label(cl_inner, text="O que há de novo — v1.0.0",
                 font=("Segoe UI", 10, "bold"),
                 bg=C["panel"], fg=C["fg_bright"]).pack(anchor="w")

        _changelog = [
            ("⊟", "Dividir PDF",
             "Separe um PDF em partes: intervalo único, múltiplos\n"
             "intervalos (campo de texto ou visual) ou todas as\n"
             "páginas individualmente."),
            ("⊞", "Juntar PDF",
             "Una múltiplos PDFs em um único arquivo. Reordene\n"
             "arrastando com o mouse ou usando os botões ↑↓.\n"
             "Suporte a drag & drop de arquivos do Explorer."),
            ("⚙", "Melhorias gerais",
             "Correções de layout, melhor gerenciamento de recursos\n"
             "e tratamento de erros aprimorado."),
        ]

        cl_f = tk.Frame(cl_inner, bg=C["panel"])
        cl_f.pack(anchor="w", pady=(10, 0))
        for icon, title, desc in _changelog:
            row_f = tk.Frame(cl_f, bg=C["panel"])
            row_f.pack(anchor="w", pady=(0, 8))
            tk.Label(row_f, text=icon,
                     font=("Segoe UI", 12), bg=C["panel"],
                     fg=C["accent"], width=3).pack(side="left", anchor="n")
            txt_f = tk.Frame(row_f, bg=C["panel"])
            txt_f.pack(side="left", anchor="w")
            tk.Label(txt_f, text=title,
                     font=("Segoe UI", 9, "bold"),
                     bg=C["panel"], fg=C["fg_bright"]).pack(anchor="w")
            tk.Label(txt_f, text=desc,
                     font=("Segoe UI", 8),
                     bg=C["panel"], fg=C["fg_dim"],
                     justify="left").pack(anchor="w")

        self.after(1500, self._auto_update_check)
```

- [ ] **Step 3: Verificar sintaxe**

```bash
python -c "import ast; ast.parse(open('pdf_ocr.py', encoding='utf-8').read()); print('OK')"
```
Esperado: `OK`

- [ ] **Step 4: Commit**

```bash
git add pdf_ocr.py
git commit -m "feat: add v1.0.0 changelog card to About page"
```

---

## Task 4: Publicar v1.0.0 no GitHub

**Files:** nenhum

- [ ] **Step 1: Push dos commits para o master**

```bash
git push origin master
```

- [ ] **Step 2: Criar e fazer push da tag v1.0.0**

```bash
git tag v1.0.0
git push origin v1.0.0
```
Esperado: GitHub Actions dispara o workflow `Build & Release`.

- [ ] **Step 3: Verificar o workflow no GitHub**

Navegar para: `https://github.com/nicolastd5/pdf-ocr/actions`

Aguardar o job `build-windows` completar (tipicamente 5–10 min). Verificar que:
- O EXE `PDF_Tools.exe` foi gerado
- A Release `v1.0.0` foi criada em `https://github.com/nicolastd5/pdf-ocr/releases`

---

## Self-Review

**Spec coverage:**
- [x] `APP_VERSION` → `"1.0.0"` — Task 1
- [x] EXE renomeado para `PDF_Tools` — Task 1
- [x] Descrições de Dividir/Juntar na aba Sobre atualizadas — Task 2
- [x] Card "O que há de novo" com changelog — Task 3
- [x] Tag `v1.0.0` criada e publicada — Task 4

**Placeholder scan:** Nenhum TBD ou TODO.

**Type consistency:** N/A — sem novos tipos ou métodos cross-task.
