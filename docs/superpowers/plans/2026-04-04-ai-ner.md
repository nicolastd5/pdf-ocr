# AI NER Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Substituir a detecção de nomes por regex por um pipeline NER de dois níveis (spaCy local + OpenAI opcional), exibindo resultados em painel na OcrPage com opção de exportar CSV.

**Architecture:** `ner.py` é um módulo independente com `NERPipeline`, `NERResult` e `Entity` — sem dependências da UI. `OcrWorker` recebe um flag `use_ner` e chama o pipeline, emitindo um novo sinal `entities`. `OcrPage` ganha controles de IA e um painel `QTreeWidget` de resultados. `SpacyInstallDialog` gerencia a instalação lazy do spaCy.

**Tech Stack:** Python 3.11+, PyQt6, spaCy `pt_core_news_lg`, openai SDK (opcional), pytesseract (existente)

---

## Mapa de Arquivos

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `pdf_ocr_qt/ner.py` | Criar | Entity, NERResult, NERPipeline (spaCy + OpenAI) |
| `pdf_ocr_qt/widgets/spacy_install.py` | Criar | SpacyInstallDialog — instala spaCy com log de progresso |
| `pdf_ocr_qt/workers.py` | Modificar | OcrWorker: flag `use_ner`, sinal `entities`, cores por tipo |
| `pdf_ocr_qt/pages/ocr.py` | Modificar | Opções de IA, painel QTreeWidget, exportar CSV |
| `pdf_ocr_qt/main.py` | Modificar | `_load_prefs`/`_save_prefs` com 4 novas chaves |
| `requirements.txt` | Modificar | Adicionar spacy e openai comentados |

---

### Task 1: Estruturas de dados e NERPipeline (spaCy apenas)

**Files:**
- Create: `pdf_ocr_qt/ner.py`
- Create: `tests/test_ner.py`

- [ ] **Step 1: Criar `tests/test_ner.py` com testes das estruturas de dados**

```python
# tests/test_ner.py
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from pdf_ocr_qt.ner import Entity, NERResult, ENTITY_TYPES


def test_entity_fields():
    e = Entity(text="João Silva", type="PER", page=1, bbox=(10, 20, 80, 15))
    assert e.text == "João Silva"
    assert e.type == "PER"
    assert e.page == 1
    assert e.bbox == (10, 20, 80, 15)


def test_ner_result_empty():
    r = NERResult(entities=[])
    assert r.entities == []


def test_ner_result_with_entities():
    e1 = Entity("Ana", "PER", 1, (0, 0, 30, 12))
    e2 = Entity("São Paulo", "LOC", 2, (5, 5, 60, 12))
    r = NERResult(entities=[e1, e2])
    assert len(r.entities) == 2
    assert r.entities[0].type == "PER"


def test_entity_types_keys():
    for k in ("PER", "ORG", "LOC", "MISC"):
        assert k in ENTITY_TYPES
```

- [ ] **Step 2: Verificar que os testes falham**

```bash
python -m pytest tests/test_ner.py -v
```
Esperado: `ModuleNotFoundError: No module named 'pdf_ocr_qt.ner'`

- [ ] **Step 3: Criar `pdf_ocr_qt/ner.py` com dataclasses e pipeline spaCy**

```python
# pdf_ocr_qt/ner.py
from __future__ import annotations
import re
import sys
import subprocess
from dataclasses import dataclass, field

ENTITY_TYPES: dict[str, str] = {
    "PER":  "Pessoa",
    "ORG":  "Organização",
    "LOC":  "Local",
    "MISC": "Outro",
}

# Mapeamento de labels spaCy → tipos internos
_SPACY_LABEL_MAP: dict[str, str] = {
    "PER":  "PER",
    "PERSON": "PER",
    "ORG":  "ORG",
    "GPE":  "LOC",
    "LOC":  "LOC",
    "MISC": "MISC",
}


@dataclass
class Entity:
    text: str
    type: str        # "PER", "ORG", "LOC", "MISC"
    page: int        # 1-based
    bbox: tuple      # (x, y, w, h) em pixels — pode ser (0,0,0,0) se sem match


@dataclass
class NERResult:
    entities: list[Entity] = field(default_factory=list)


class SpacyNotInstalledError(RuntimeError):
    pass


class NERPipeline:
    def __init__(self, use_openai: bool = False, openai_key: str = "",
                 engine: str = "spacy"):
        self.use_openai  = use_openai
        self.openai_key  = openai_key
        self.engine      = engine
        self._nlp        = None   # lazy-loaded

    # ── Instalação estática ──────────────────────────────────────
    @staticmethod
    def is_spacy_installed() -> bool:
        try:
            import spacy  # noqa: F401
            spacy.load("pt_core_news_lg")
            return True
        except Exception:
            return False

    @staticmethod
    def install_spacy():
        """Gerador: yields linhas de output durante a instalação."""
        pip = [sys.executable, "-m", "pip", "install", "spacy>=3.7"]
        proc = subprocess.Popen(pip, stdout=subprocess.PIPE,
                                stderr=subprocess.STDOUT, text=True)
        for line in proc.stdout:
            yield line.rstrip()
        proc.wait()
        if proc.returncode != 0:
            yield "ERRO: falha ao instalar spaCy."
            return
        dl = [sys.executable, "-m", "spacy", "download", "pt_core_news_lg"]
        proc2 = subprocess.Popen(dl, stdout=subprocess.PIPE,
                                 stderr=subprocess.STDOUT, text=True)
        for line in proc2.stdout:
            yield line.rstrip()
        proc2.wait()
        if proc2.returncode == 0:
            yield "OK"
        else:
            yield "ERRO: falha ao baixar pt_core_news_lg."

    # ── Extração principal ────────────────────────────────────────
    def extract(self, ocr_data: dict, page_num: int,
                img_w: int, img_h: int) -> NERResult:
        if self.engine == "spacy":
            result = self._extract_spacy(ocr_data, page_num, img_w, img_h)
        else:
            result = NERResult()

        if self.use_openai and self.openai_key:
            result = self._enrich_openai(ocr_data, page_num, result)

        return result

    def _extract_spacy(self, ocr_data: dict, page_num: int,
                       img_w: int, img_h: int) -> NERResult:
        try:
            import spacy
        except ImportError:
            raise SpacyNotInstalledError(
                "spaCy não está instalado. Use NERPipeline.install_spacy().")

        if self._nlp is None:
            try:
                self._nlp = spacy.load("pt_core_news_lg")
            except OSError:
                raise SpacyNotInstalledError(
                    "Modelo pt_core_news_lg não encontrado. "
                    "Execute: python -m spacy download pt_core_news_lg")

        texts   = ocr_data.get("text", [])
        confs   = ocr_data.get("conf", [])
        lefts   = ocr_data.get("left", [])
        tops    = ocr_data.get("top", [])
        widths  = ocr_data.get("width", [])
        heights = ocr_data.get("height", [])

        # Monta texto da página e índice de tokens para bbox
        token_bboxes: list[tuple[int, int, int, int]] = []
        words: list[str] = []
        for i, word in enumerate(texts):
            if not word or not word.strip():
                continue
            try:
                conf = int(confs[i])
            except (TypeError, ValueError):
                continue
            if conf < 30:
                continue
            words.append(word)
            token_bboxes.append((lefts[i], tops[i], widths[i], heights[i]))

        full_text = " ".join(words)
        doc = self._nlp(full_text)

        entities: list[Entity] = []
        char_offset = 0
        word_char_starts = []
        for w in words:
            word_char_starts.append(char_offset)
            char_offset += len(w) + 1  # +1 for space

        for ent in doc.ents:
            ent_type = _SPACY_LABEL_MAP.get(ent.label_, "MISC")
            # Tenta encontrar bbox pelo match de texto nos tokens
            bbox = self._find_bbox(ent.text, words, token_bboxes, word_char_starts,
                                   ent.start_char, ent.end_char)
            entities.append(Entity(
                text=ent.text,
                type=ent_type,
                page=page_num,
                bbox=bbox,
            ))

        return NERResult(entities=entities)

    def _find_bbox(self, ent_text: str, words: list[str],
                   token_bboxes: list[tuple], word_char_starts: list[int],
                   start_char: int, end_char: int) -> tuple:
        """Retorna bbox que engloba todos os tokens da entidade."""
        matched = []
        for i, (w, cs) in enumerate(zip(words, word_char_starts)):
            ce = cs + len(w)
            if cs >= start_char and ce <= end_char + 1:
                matched.append(token_bboxes[i])
        if not matched:
            return (0, 0, 0, 0)
        x  = min(b[0] for b in matched)
        y  = min(b[1] for b in matched)
        x2 = max(b[0] + b[2] for b in matched)
        y2 = max(b[1] + b[3] for b in matched)
        return (x, y, x2 - x, y2 - y)

    def _enrich_openai(self, ocr_data: dict, page_num: int,
                       base: NERResult) -> NERResult:
        """Enriquece result do spaCy com entidades do OpenAI GPT-4o-mini."""
        try:
            from openai import OpenAI
        except ImportError:
            return base

        texts  = ocr_data.get("text", [])
        confs  = ocr_data.get("conf", [])
        words  = [w for w, c in zip(texts, confs)
                  if w and w.strip() and _safe_int(c) >= 30]
        page_text = " ".join(words)
        if not page_text.strip():
            return base

        client = OpenAI(api_key=self.openai_key)
        prompt = (
            "Extraia entidades nomeadas do texto abaixo. "
            "Responda SOMENTE com JSON array: "
            '[{"text":"...","type":"PER|ORG|LOC|MISC"}]\n\n'
            + page_text
        )
        try:
            resp = client.chat.completions.create(
                model="gpt-4o-mini",
                messages=[{"role": "user", "content": prompt}],
                temperature=0,
                max_tokens=512,
            )
            import json
            raw = resp.choices[0].message.content.strip()
            # Remove possível markdown fence
            raw = re.sub(r"^```[a-z]*\n?", "", raw)
            raw = re.sub(r"\n?```$", "", raw)
            items = json.loads(raw)
        except Exception:
            return base

        existing_texts = {e.text.lower() for e in base.entities}
        lefts   = ocr_data.get("left", [])
        tops    = ocr_data.get("top", [])
        widths  = ocr_data.get("width", [])
        heights = ocr_data.get("height", [])

        word_list  = [w for w, c in zip(texts, confs)
                      if w and w.strip() and _safe_int(c) >= 30]
        bbox_list  = [(lefts[i], tops[i], widths[i], heights[i])
                      for i, (w, c) in enumerate(zip(texts, confs))
                      if w and w.strip() and _safe_int(c) >= 30]
        char_starts: list[int] = []
        off = 0
        for w in word_list:
            char_starts.append(off)
            off += len(w) + 1

        new_entities = list(base.entities)
        for item in items:
            ent_text = item.get("text", "").strip()
            ent_type = item.get("type", "MISC")
            if not ent_text or ent_text.lower() in existing_texts:
                continue
            if ent_type not in ENTITY_TYPES:
                ent_type = "MISC"
            # Tenta encontrar bbox
            idx = next((i for i, w in enumerate(word_list)
                        if ent_text.lower().startswith(w.lower())), -1)
            if idx >= 0:
                cs = char_starts[idx]
                ce = cs + len(ent_text)
                bbox = NERPipeline._static_find_bbox(
                    ent_text, word_list, bbox_list, char_starts, cs, ce)
            else:
                bbox = (0, 0, 0, 0)
            new_entities.append(Entity(
                text=ent_text, type=ent_type, page=page_num, bbox=bbox))
            existing_texts.add(ent_text.lower())

        return NERResult(entities=new_entities)

    @staticmethod
    def _static_find_bbox(ent_text, words, token_bboxes, word_char_starts,
                          start_char, end_char):
        matched = []
        for i, (w, cs) in enumerate(zip(words, word_char_starts)):
            ce = cs + len(w)
            if cs >= start_char and ce <= end_char + 1:
                matched.append(token_bboxes[i])
        if not matched:
            return (0, 0, 0, 0)
        x  = min(b[0] for b in matched)
        y  = min(b[1] for b in matched)
        x2 = max(b[0] + b[2] for b in matched)
        y2 = max(b[1] + b[3] for b in matched)
        return (x, y, x2 - x, y2 - y)


def _safe_int(v) -> int:
    try:
        return int(v)
    except (TypeError, ValueError):
        return -1
```

- [ ] **Step 4: Rodar os testes**

```bash
python -m pytest tests/test_ner.py -v
```
Esperado: 4 testes PASS.

- [ ] **Step 5: Commit**

```bash
git add pdf_ocr_qt/ner.py tests/test_ner.py
git commit -m "feat: add NERPipeline with Entity/NERResult dataclasses"
```

---

### Task 2: SpacyInstallDialog

**Files:**
- Create: `pdf_ocr_qt/widgets/spacy_install.py`

- [ ] **Step 1: Criar `pdf_ocr_qt/widgets/spacy_install.py`**

```python
# pdf_ocr_qt/widgets/spacy_install.py
from PyQt6.QtWidgets import (
    QDialog, QVBoxLayout, QHBoxLayout, QLabel,
    QPushButton, QPlainTextEdit
)
from PyQt6.QtCore import QThread, pyqtSignal, Qt
from pdf_ocr_qt.styles import accent_btn, flat_btn, C
from pdf_ocr_qt.ner import NERPipeline


class _InstallThread(QThread):
    line   = pyqtSignal(str)
    done   = pyqtSignal(bool)   # True = sucesso

    def run(self):
        success = True
        for output in NERPipeline.install_spacy():
            self.line.emit(output)
            if output.startswith("ERRO"):
                success = False
        self.done.emit(success)


class SpacyInstallDialog(QDialog):
    """Diálogo modal que instala spaCy + pt_core_news_lg com log de progresso."""

    def __init__(self, parent=None):
        super().__init__(parent)
        self.setWindowTitle("Instalar spaCy")
        self.setMinimumWidth(500)
        self.setStyleSheet(
            f"background: {C['panel']}; color: {C['fg']}; font-size: 13px;")
        self._success = False
        self._thread: _InstallThread | None = None
        self._build_ui()

    @property
    def install_succeeded(self) -> bool:
        return self._success

    def _build_ui(self):
        layout = QVBoxLayout(self)
        layout.setSpacing(12)

        self._msg = QLabel(
            "O spaCy não está instalado.\n"
            "Deseja instalar agora? (~50 MB)")
        self._msg.setAlignment(Qt.AlignmentFlag.AlignLeft)
        layout.addWidget(self._msg)

        self._log = QPlainTextEdit()
        self._log.setReadOnly(True)
        self._log.setMaximumHeight(180)
        self._log.setStyleSheet(
            f"background: {C['input']}; color: {C['fg_dim']};"
            f" font-family: Consolas, monospace; font-size: 11px;"
            f" border: 1px solid {C['border']};")
        self._log.setVisible(False)
        layout.addWidget(self._log)

        btn_row = QHBoxLayout()
        self._btn_install = accent_btn("Instalar")
        self._btn_install.clicked.connect(self._start_install)
        self._btn_cancel  = flat_btn("Cancelar")
        self._btn_cancel.clicked.connect(self.reject)
        btn_row.addWidget(self._btn_install)
        btn_row.addWidget(self._btn_cancel)
        btn_row.addStretch()
        layout.addLayout(btn_row)

    def _start_install(self):
        self._btn_install.setEnabled(False)
        self._btn_cancel.setEnabled(False)
        self._log.setVisible(True)
        self._msg.setText("Instalando spaCy e modelo PT…")

        self._thread = _InstallThread(self)
        self._thread.line.connect(self._log.appendPlainText)
        self._thread.done.connect(self._on_done)
        self._thread.start()

    def _on_done(self, success: bool):
        self._success = success
        self._btn_cancel.setEnabled(True)
        if success:
            self._msg.setText("Instalação concluída com sucesso!")
            self.accept()
        else:
            self._msg.setText("Erro durante a instalação. Veja o log acima.")
            self._btn_install.setEnabled(True)
            self._btn_install.setText("Tentar novamente")
```

- [ ] **Step 2: Verificar sintaxe**

```bash
python -c "from pdf_ocr_qt.widgets.spacy_install import SpacyInstallDialog; print('OK')"
```
Esperado: `OK`

- [ ] **Step 3: Commit**

```bash
git add pdf_ocr_qt/widgets/spacy_install.py
git commit -m "feat: add SpacyInstallDialog for lazy spaCy installation"
```

---

### Task 3: OcrWorker — sinal entities e cores por tipo

**Files:**
- Modify: `pdf_ocr_qt/workers.py`

- [ ] **Step 1: Ler o arquivo atual para referência**

Abrir `pdf_ocr_qt/workers.py` e localizar a classe `OcrWorker`. Pontos de modificação:
- Linha ~17: declaração dos sinais — adicionar `entities`
- Linha ~22: `__init__` — adicionar `use_ner`, `use_openai`, `openai_key`, `ner_engine`
- Linha ~31: `run()` — instanciar `NERPipeline` se `use_ner`
- Linha ~95: `_process_single` — acumular entidades e usar cores por tipo

- [ ] **Step 2: Adicionar sinal `entities` e parâmetros NER ao `__init__`**

Substituir o bloco de `__init__` da `OcrWorker` (de `def __init__` até o final do método) pelo código abaixo:

```python
    entities = pyqtSignal(list)   # list[Entity], emitido após finished

    def __init__(self, files, outdir, lang, highlight_names,
                 poppler_path, use_ner=False, use_openai=False,
                 openai_key="", ner_engine="spacy", parent=None):
        super().__init__(parent)
        self.files           = files
        self.outdir          = outdir
        self.lang            = lang
        self.highlight_names = highlight_names
        self.poppler_path    = poppler_path
        self.use_ner         = use_ner
        self.use_openai      = use_openai
        self.openai_key      = openai_key
        self.ner_engine      = ner_engine
```

- [ ] **Step 3: Modificar `run()` para instanciar NERPipeline e acumular entidades**

Substituir o método `run()` pelo código abaixo:

```python
    def run(self):
        from pdf_ocr_qt.ner import NERPipeline
        pipeline = None
        if self.use_ner:
            pipeline = NERPipeline(
                use_openai=self.use_openai,
                openai_key=self.openai_key,
                engine=self.ner_engine,
            )

        total = len(self.files)
        ok_files, errors = [], []
        all_entities = []

        for fi, input_pdf in enumerate(self.files, 1):
            base     = os.path.splitext(os.path.basename(input_pdf))[0]
            dest_dir = self.outdir if self.outdir else os.path.dirname(input_pdf)
            output_pdf = os.path.join(dest_dir, base + "_pesquisavel.pdf")
            try:
                page_entities = self._process_single(
                    input_pdf, output_pdf, fi, total, pipeline)
                ok_files.append(output_pdf)
                all_entities.extend(page_entities)
            except Exception as e:
                errors.append(f"{os.path.basename(input_pdf)}: {e}")

        self.finished.emit(ok_files, errors)
        if self.use_ner:
            self.entities.emit(all_entities)
```

- [ ] **Step 4: Modificar `_process_single` para aceitar pipeline e usar cores por tipo**

Substituir a assinatura e o bloco de destaque do método `_process_single`. A assinatura muda de:
```python
    def _process_single(self, input_pdf, output_pdf, fi, total_files):
```
para:
```python
    def _process_single(self, input_pdf, output_pdf, fi, total_files, pipeline=None):
```

Substituir o bloco de destaque (linhas com `if self.highlight_names:` e o draw do rect) pelo código abaixo:

```python
                # ── Destaque de entidades ─────────────────────────
                if pipeline is not None:
                    from pdf_ocr_qt.ner import ENTITY_TYPES
                    ner_result = pipeline.extract(ocr_data, pi, img_w, img_h)
                    _COLORS = {
                        "PER":  (1.0, 0.85, 0.0, 0.35),
                        "ORG":  (0.2, 0.6,  1.0, 0.30),
                        "LOC":  (0.2, 0.85, 0.4, 0.30),
                        "MISC": (0.6, 0.6,  0.6, 0.25),
                    }
                    boxes_by_type: dict[str, list] = {k: [] for k in ENTITY_TYPES}
                    for ent in ner_result.entities:
                        bx, by, bw, bh = ent.bbox
                        if bw > 0 and bh > 0:
                            boxes_by_type[ent.type].append((bx, by, bw, bh))
                    c.saveState()
                    for etype, boxes in boxes_by_type.items():
                        r, g, b, a = _COLORS.get(etype, (1.0, 0.85, 0.0, 0.35))
                        c.setFillColorRGB(r, g, b, alpha=a)
                        for nx, ny, nw, nh in boxes:
                            pad = 2
                            c.rect(nx - pad, img_h - ny - nh - pad,
                                   nw + pad * 2, nh + pad * 2,
                                   fill=1, stroke=0)
                    c.restoreState()
                elif self.highlight_names:
                    # Fallback: regex original
                    name_boxes = self._detect_names(ocr_data)
                    if name_boxes:
                        c.saveState()
                        c.setFillColorRGB(1.0, 0.85, 0.0, alpha=0.35)
                        for nx, ny, nw, nh in name_boxes:
                            pad = 2
                            c.rect(nx - pad, img_h - ny - nh - pad,
                                   nw + pad * 2, nh + pad * 2,
                                   fill=1, stroke=0)
                        c.restoreState()
```

Também substituir o final do loop interno para retornar as entidades coletadas. Ao final de `_process_single`, antes do `with open(output_pdf, "wb")`, acumular em `page_entities`. A função deve retornar `page_entities`. Substitua o bloco final do método:

```python
            with open(output_pdf, "wb") as f:
                merger.write(f)
        finally:
            try:
                merger.close()
            except Exception:
                pass
        return page_entities
```

E inicializar no início de `_process_single` (logo após `basename = ...`):
```python
        page_entities = []
```

E acumular dentro do loop de páginas, após o bloco de destaque NER:
```python
                if pipeline is not None:
                    page_entities.extend(ner_result.entities)
```

- [ ] **Step 5: Verificar sintaxe**

```bash
python -c "from pdf_ocr_qt.workers import OcrWorker; print('OK')"
```
Esperado: `OK`

- [ ] **Step 6: Commit**

```bash
git add pdf_ocr_qt/workers.py
git commit -m "feat: OcrWorker — add NER pipeline integration and entities signal"
```

---

### Task 4: Preferências NER no MainWindow

**Files:**
- Modify: `pdf_ocr_qt/main.py`

- [ ] **Step 1: Adicionar novas chaves em `_load_prefs`**

Localizar o método `_load_prefs` em `pdf_ocr_qt/main.py`. Após a linha `self._prefs = {}` (bloco except), adicionar o método de defaults logo abaixo do except, ou criar um helper. Na prática, modificar `_load_prefs` para garantir defaults:

Substituir `_load_prefs` por:

```python
    def _load_prefs(self):
        try:
            with open(_prefs_path()) as f:
                self._prefs = json.load(f)
        except Exception:
            self._prefs = {}
        # Garantir chaves NER com defaults
        self._prefs.setdefault("use_ner",    False)
        self._prefs.setdefault("use_openai", False)
        self._prefs.setdefault("openai_key", "")
        self._prefs.setdefault("ner_engine", "spacy")
```

- [ ] **Step 2: Verificar sintaxe**

```bash
python -c "from pdf_ocr_qt.main import MainWindow; print('OK')"
```
Esperado: `OK`

- [ ] **Step 3: Commit**

```bash
git add pdf_ocr_qt/main.py
git commit -m "feat: add NER preference keys with defaults in _load_prefs"
```

---

### Task 5: OcrPage — opções de IA e painel de resultados

**Files:**
- Modify: `pdf_ocr_qt/pages/ocr.py`

Esta é a maior tarefa. Dividida em sub-passos para clareza.

- [ ] **Step 1: Adicionar imports necessários no topo de `ocr.py`**

Localizar o bloco de imports no topo de `pdf_ocr_qt/pages/ocr.py` e adicionar:

```python
import csv
from PyQt6.QtWidgets import (
    QWidget, QVBoxLayout, QHBoxLayout, QLabel, QListWidget,
    QCheckBox, QComboBox, QFileDialog, QMessageBox, QFrame,
    QLineEdit, QTreeWidget, QTreeWidgetItem, QSizePolicy
)
from PyQt6.QtGui import QColor, QIcon, QPixmap
```

(Substituir a linha de import existente do `QWidget ... QFrame` pela versão expandida acima.)

- [ ] **Step 2: Adicionar atributos NER ao `__init__` de `OcrPage`**

Logo após `self._spinner = SpinnerDialog(self)` no `__init__`, adicionar:

```python
        self._entities: list = []
```

- [ ] **Step 3: Adicionar bloco de opções de IA em `_build_ui`**

Localizar no método `_build_ui` a linha:
```python
        layout.addStretch()
```
(que fica após os checkboxes de destaque e destino). Substituir por:

```python
        # ── Opções de IA ──────────────────────────────────────────
        self._chk_ner = QCheckBox("Usar IA para detectar nomes (spaCy)")
        self._chk_ner.toggled.connect(self._on_ner_toggled)
        layout.addWidget(self._chk_ner)

        ai_indent = QHBoxLayout()
        ai_indent.setContentsMargins(24, 0, 0, 0)
        ai_col = QVBoxLayout()

        self._chk_openai = QCheckBox("Usar OpenAI para NER avançado")
        self._chk_openai.setEnabled(False)
        self._chk_openai.toggled.connect(self._on_openai_toggled)
        ai_col.addWidget(self._chk_openai)

        key_row = QHBoxLayout()
        key_lbl = QLabel("Chave API OpenAI:")
        key_lbl.setObjectName("dim_lbl")
        self._key_edit = QLineEdit()
        self._key_edit.setEchoMode(QLineEdit.EchoMode.Password)
        self._key_edit.setPlaceholderText("sk-…")
        self._key_edit.setEnabled(False)
        self._key_edit.setFixedWidth(260)
        key_row.addWidget(key_lbl)
        key_row.addWidget(self._key_edit)
        key_row.addStretch()
        ai_col.addLayout(key_row)

        ai_indent.addLayout(ai_col)
        layout.addLayout(ai_indent)

        layout.addStretch()
```

- [ ] **Step 4: Adicionar painel de resultados em `_build_ui`**

Logo após o `layout.addStretch()` recém-adicionado e antes do bloco `# Status + progress`, inserir:

```python
        # ── Painel de entidades (oculto até OCR com IA) ──────────
        self._ner_panel = QFrame()
        self._ner_panel.setObjectName("drop_area")
        self._ner_panel.setVisible(False)
        ner_layout = QVBoxLayout(self._ner_panel)
        ner_layout.setContentsMargins(8, 8, 8, 8)
        ner_layout.setSpacing(6)

        ner_hdr = QHBoxLayout()
        ner_title = QLabel("Entidades encontradas")
        ner_title.setObjectName("section_lbl")
        ner_hdr.addWidget(ner_title)
        ner_hdr.addStretch()
        self._btn_export = flat_btn("📄 Exportar CSV")
        self._btn_export.clicked.connect(self._export_csv)
        ner_hdr.addWidget(self._btn_export)
        ner_layout.addLayout(ner_hdr)

        self._tree = QTreeWidget()
        self._tree.setHeaderLabels(["Nome", "Tipo", "Páginas"])
        self._tree.setColumnWidth(0, 200)
        self._tree.setColumnWidth(1, 100)
        self._tree.setColumnWidth(2, 80)
        self._tree.setFixedHeight(160)
        ner_layout.addWidget(self._tree)

        layout.addWidget(self._ner_panel)
```

- [ ] **Step 5: Adicionar métodos auxiliares de IA**

Adicionar os seguintes métodos à classe `OcrPage` (antes de `_on_progress`):

```python
    # ── Controles de IA ──────────────────────────────────────────
    def _on_ner_toggled(self, checked: bool):
        if checked:
            from pdf_ocr_qt.ner import NERPipeline
            if not NERPipeline.is_spacy_installed():
                from pdf_ocr_qt.widgets.spacy_install import SpacyInstallDialog
                dlg = SpacyInstallDialog(self)
                dlg.exec()
                if not dlg.install_succeeded:
                    self._chk_ner.blockSignals(True)
                    self._chk_ner.setChecked(False)
                    self._chk_ner.blockSignals(False)
                    return
        self._chk_openai.setEnabled(checked)
        if not checked:
            self._chk_openai.setChecked(False)
            self._key_edit.setEnabled(False)

    def _on_openai_toggled(self, checked: bool):
        self._key_edit.setEnabled(checked)

    def _color_icon(self, etype: str) -> QIcon:
        """Retorna um ícone 12x12 colorido por tipo de entidade."""
        _COLORS = {
            "PER":  "#f59e0b",
            "ORG":  "#38bdf8",
            "LOC":  "#34d399",
            "MISC": "#94a3b8",
        }
        color = _COLORS.get(etype, "#94a3b8")
        pix = QPixmap(12, 12)
        pix.fill(QColor(color))
        return QIcon(pix)

    def _populate_tree(self, entities: list):
        """Preenche o QTreeWidget com as entidades encontradas."""
        from pdf_ocr_qt.ner import ENTITY_TYPES
        self._tree.clear()
        # Agrupa por texto para consolidar páginas
        seen: dict[str, dict] = {}
        for ent in entities:
            key = (ent.text.lower(), ent.type)
            if key not in seen:
                seen[key] = {"text": ent.text, "type": ent.type, "pages": set()}
            seen[key]["pages"].add(ent.page)

        for data in seen.values():
            pages_str = ", ".join(str(p) for p in sorted(data["pages"]))
            item = QTreeWidgetItem([
                data["text"],
                ENTITY_TYPES.get(data["type"], data["type"]),
                pages_str,
            ])
            item.setIcon(0, self._color_icon(data["type"]))
            self._tree.addTopLevelItem(item)

    def _export_csv(self):
        """Exporta as entidades para CSV."""
        if not self._entities:
            return
        from pdf_ocr_qt.ner import ENTITY_TYPES

        # Sugere caminho baseado no primeiro arquivo da lista
        default_dir = ""
        if self._files:
            base = os.path.splitext(os.path.basename(self._files[0]))[0]
            default_dir = os.path.join(
                os.path.dirname(self._files[0]),
                f"{base}_entidades.csv")

        path, _ = QFileDialog.getSaveFileName(
            self, "Exportar entidades", default_dir,
            "CSV (*.csv)")
        if not path:
            return

        # Consolida por (texto, tipo) para ter todas as páginas
        seen: dict[tuple, dict] = {}
        for ent in self._entities:
            key = (ent.text.lower(), ent.type)
            if key not in seen:
                seen[key] = {
                    "text":  ent.text,
                    "type":  ENTITY_TYPES.get(ent.type, ent.type),
                    "pages": set(),
                    "file":  (os.path.basename(self._files[0])
                              if self._files else ""),
                }
            seen[key]["pages"].add(ent.page)

        try:
            with open(path, "w", newline="", encoding="utf-8") as f:
                writer = csv.writer(f)
                writer.writerow(["Nome", "Tipo", "Páginas", "Arquivo"])
                for data in seen.values():
                    pages_str = ", ".join(str(p) for p in sorted(data["pages"]))
                    writer.writerow([
                        data["text"], data["type"],
                        pages_str, data["file"]])
            QMessageBox.information(self, "CSV exportado",
                f"Entidades salvas em:\n{path}")
        except Exception as e:
            QMessageBox.critical(self, "Erro ao exportar", str(e))
```

- [ ] **Step 6: Modificar `_start` para passar parâmetros NER ao OcrWorker**

Localizar o bloco onde `OcrWorker` é instanciado em `_start`. Substituir:

```python
        self._worker = OcrWorker(
            list(self._files), out_dir or "",
            lang, highlight, find_poppler(), self)
        self._worker.progress.connect(self._on_progress)
        self._worker.finished.connect(self._on_finished)
        self._worker.error.connect(self._on_error)
        self._worker.start()
```

por:

```python
        use_ner    = self._chk_ner.isChecked()
        use_openai = self._chk_openai.isChecked()
        openai_key = self._key_edit.text().strip()

        self._worker = OcrWorker(
            list(self._files), out_dir or "",
            lang, highlight, find_poppler(),
            use_ner=use_ner,
            use_openai=use_openai,
            openai_key=openai_key,
            parent=self)
        self._worker.progress.connect(self._on_progress)
        self._worker.finished.connect(self._on_finished)
        self._worker.error.connect(self._on_error)
        self._worker.entities.connect(self._on_entities)
        self._worker.start()
```

- [ ] **Step 7: Adicionar slot `_on_entities`**

Adicionar após `_on_error`:

```python
    def _on_entities(self, entities: list):
        self._entities = entities
        if entities:
            self._populate_tree(entities)
            self._ner_panel.setVisible(True)
```

- [ ] **Step 8: Carregar preferências NER na OcrPage**

Adicionar método `load_prefs` à `OcrPage` (chamado pelo `MainWindow` após instanciar a página):

```python
    def load_prefs(self, prefs: dict):
        self._chk_ner.setChecked(prefs.get("use_ner", False))
        self._chk_openai.setChecked(prefs.get("use_openai", False))
        self._key_edit.setText(prefs.get("openai_key", ""))

    def save_prefs(self, prefs: dict):
        prefs["use_ner"]    = self._chk_ner.isChecked()
        prefs["use_openai"] = self._chk_openai.isChecked()
        prefs["openai_key"] = self._key_edit.text().strip()
```

- [ ] **Step 9: Chamar `load_prefs`/`save_prefs` no MainWindow**

Em `pdf_ocr_qt/main.py`, no método `_build_ui`, após instanciar `OcrPage`:

```python
        for key, PageClass in [...]:
            page = PageClass(self)
            self._pages[key] = page
            self._stack.addWidget(page)

        # Carregar prefs nas páginas que as suportam
        ocr_page = self._pages["ocr"]
        if hasattr(ocr_page, "load_prefs"):
            ocr_page.load_prefs(self._prefs)
```

E em `closeEvent`:

```python
    def closeEvent(self, event):
        ocr_page = self._pages.get("ocr")
        if ocr_page and hasattr(ocr_page, "save_prefs"):
            ocr_page.save_prefs(self._prefs)
        self._save_prefs()
        super().closeEvent(event)
```

- [ ] **Step 10: Verificar sintaxe de todos os arquivos modificados**

```bash
python -c "from pdf_ocr_qt.pages.ocr import OcrPage; print('OK')"
python -c "from pdf_ocr_qt.main import MainWindow; print('OK')"
```
Esperado: `OK` nos dois.

- [ ] **Step 11: Commit**

```bash
git add pdf_ocr_qt/pages/ocr.py pdf_ocr_qt/main.py
git commit -m "feat: OcrPage — AI NER options, results panel, CSV export"
```

---

### Task 6: requirements.txt e smoke test final

**Files:**
- Modify: `requirements.txt`

- [ ] **Step 1: Adicionar dependências opcionais comentadas**

Substituir o conteúdo de `requirements.txt` por:

```
PyQt6
pytesseract
Pillow
pdf2image
reportlab
PyPDF2
pdf2docx
pyinstaller
# Tesseract OCR e Poppler são embutidos automaticamente pelo GitHub Actions

# ── IA / NER (opcional) — descomentar para ativar: ──────────────
# spacy>=3.7
# openai>=1.0
# Após instalar spacy: python -m spacy download pt_core_news_lg
```

- [ ] **Step 2: Smoke test — rodar o app**

```bash
python -m pdf_ocr_qt.main
```
Esperado: app abre normalmente, página OCR mostra o checkbox "Usar IA para detectar nomes (spaCy)" abaixo das opções existentes, painel de entidades está oculto.

- [ ] **Step 3: Rodar todos os testes**

```bash
python -m pytest tests/ -v
```
Esperado: todos os testes PASS.

- [ ] **Step 4: Commit final**

```bash
git add requirements.txt
git commit -m "chore: document optional spaCy/openai deps in requirements.txt"
```

- [ ] **Step 5: Tag de versão**

```bash
git tag v1.0.8
```

---

## Notas para o implementador

- **Ordem importa:** Tasks 1 → 2 → 3 → 4 → 5 → 6. Cada task depende da anterior.
- **spaCy não instalado:** Ao testar localmente sem spaCy, o checkbox "Usar IA" abrirá o `SpacyInstallDialog`. Para pular e testar só a UI, deixe o checkbox desmarcado.
- **OpenAI:** Requer chave válida e `pip install openai`. Para testar sem chave, deixe o campo vazio — o nível 2 simplesmente não é chamado.
- **Compatibilidade retroativa:** OCR sem IA ativada funciona exatamente igual ao v1.0.7 — nenhuma regressão possível no caminho `use_ner=False`.
